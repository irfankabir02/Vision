name: Secrets Gate

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  secrets-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: TruffleHog secret scan
        uses: trufflesecurity/trufflehog@v3.94.0
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha || format('{0}~1', github.sha) }}
          head: ${{ github.event.pull_request.head.sha || github.sha }}
          extra_args: --only-verified --fail --config .trufflehog.yml

      - name: Block tracked env files
        run: |
          tracked_env=$(git ls-files | grep -E '(^|/)\.env($|\.)' || true)
          if [ -n "$tracked_env" ]; then
            echo "::error::Tracked env file detected:"
            echo "$tracked_env"
            exit 1
          fi
